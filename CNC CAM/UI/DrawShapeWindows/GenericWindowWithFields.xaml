<Window x:Class="CNC_CAM.UI.DrawShapeWindows.GenericWindowWithFields"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="DrawCircleWindow" Height="auto" Width="auto" SizeToContent="WidthAndHeight">
    <Grid>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Stretch"
                    Margin="0 0 0 5">
            <StackPanel x:Name="FieldsStack"
                        Orientation="Vertical">
            </StackPanel>
            <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" Margin="10 10 10 0">
                <Button x:Name="DoneButton"
                        Height="20"
                        Width="70" Click="DoneButton_OnClick">
                    Done
                </Button>
                <Button x:Name="CancelButton"
                        Height="20"
                        Width="70"
                        Margin="0 5 0 0" Click="CancelButton_OnClick">
                    Cancel
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>